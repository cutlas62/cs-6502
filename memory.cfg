MEMORY {
	RAM: start = $0000, size = $4000, type = rw;
	ROM: start = $8000, size = $7F00, type = ro, fill = yes;
	WOZMON: start = $FF00, size = $FA, type = ro, fill = yes;
	RESET_VECTORS: start = $FFFA, size = 6, type = ro, fill = yes;
}

SEGMENTS {
	BIOS: load = "ROM", type  = ro, align = $100;
	HELLO_WORLD: load = "ROM", type = ro, align = $100;
	WOZMON: load = "WOZMON", type = ro;
	RESET_VECTORS: load = "RESET_VECTORS", type = ro;
}